---
allow:
  - whitelisted
  - contact
deny:
  - blocked
onboard:
  invite_code:
    - REPLACE_WITH_A_PRIVATE_INVITE_CODE
default: deny
---

# ConnectOnion host trust policy

Copy this file to `.co/trust-policy.md` and replace the placeholder locally.
The `.co` directory is ignored by Git, so real invite codes and trust state stay
outside source control.
